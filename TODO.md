# Short-term Actionable Tasks

## High Priority
~~1. **Modernize Boost Filesystem Usage**: Update deprecated Boost Filesystem v2 calls (e.g., `branch_path()`) to modern v3 equivalents (e.g., `parent_path()`) to eliminate compilation warnings and prepare for newer Boost versions.~~ (Completed)
~~2. **Settings GUI Linkage**: Ensure the `wxWidgets` settings GUI compiles and integrates cleanly alongside the main executable without configuration errors.~~ (Completed)
~~3. **Verify Rendering Pipeline**: Test the OpenGL rendering path on a machine with a display context to ensure textures are correctly uploaded via the modernized FFmpeg decoding pipeline.~~ (Completed)

## Medium Priority
~~4. **Remove Unused GLEW Dependencies**: Fully remove any lingering system `libGLEW` linking flags if they are no longer required due to the `GLee` reversion.~~ (Completed)
~~5. **Code Cleanup**: Address `register` storage class warnings generated by modern GCC implementations on older C code (`Common/isaac.cpp`).~~ (Completed)
6. **Edge Case Verification**: Continue to test headless/multi-monitor detection edge cases with the new ImGui integration.
7. **Cross-Platform Verification**: Spin up test environments for MacOS and Windows to confirm standard compilation.

## Low Priority
6. **Documentation**: Add inline comments to the newly updated FFmpeg decoding steps explaining the transition from the old packet/frame API.
